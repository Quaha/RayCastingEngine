#pragma once

#include "libs.hpp"

const long double USER_MOVE_SPEED = 0.2;
const long double USER_ROTATE_SPEED = 0.005; // [0, 2PI)

const long double FOV = 90; //[0, 360]
const long double RAYS_STEP = 0.5;
const int RAYS_LENGTH = 1000;

const unsigned int WINDOW_WIDTH  = 1280;
const unsigned int WINDOW_HEIGHT = 720;